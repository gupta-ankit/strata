<%= form_with(model: strata_plan) do |form| %>
  <% if strata_plan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(strata_plan.errors.count, "error") %> prohibited this strata_plan from being saved:</h2>

      <ul>
        <% strata_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
