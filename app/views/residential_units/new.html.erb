<ul>
  <% @residential_unit.errors.full_messages.each do |m| %>
    <li><%= m %></li>
  <% end %>
</ul>

<%= form_with model: [@strata_plan, @residential_unit] do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.submit "Add" %>
  </div>
<% end %>